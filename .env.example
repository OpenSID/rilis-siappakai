APP_NAME='Dasbor SiapPakai'
APP_ENV={$app_env}
APP_KEY=
APP_DEBUG={$app_debug}
APP_URL={$app_url}
ASSET_URL=null

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST={$db_host}
DB_PORT={$db_port}
DB_DATABASE=phpmyadmin
DB_USERNAME={$db_username}
DB_PASSWORD={$db_password}

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=public
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_USERNAME=null
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=null
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

#Root Dasbor SiapPakai
ROOT_OPENSID={$dirRoot}
MULTISITE_OPENSID={$dirRoot}multisite/

#github token
GIT_TOKEN={$token_github}

#FTP
FTP_USER={$ftp_user}
FTP_PASS={$ftp_pass}

#istilah OpenKab disini yang dimaksud adalah OpenSID Database Gabungan
OPENKAB={$openkab}

#pantau
API_PANTAU=
TOKEN_PANTAU=

#server Layanan
SERVER_LAYANAN=https://devlayanan.opendesa.id
RELOAD_APACHE=false

#Versi Beta
BETA_OPENSID=false
BETA_PBB=false
BETA_API=false

#Sentry Error Tracking
SENTRY_DSN=https://07fae2335410b4be73ed509ea0dff825@o4509819952037888.ingest.us.sentry.io/4509819957346304
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1
BETA_OPENDK=false

# ============================================
# CLOUDFLARE API CONFIGURATION
# ============================================

# Default IP for DNS validation (Optional)
# Public IP of this server for DNS validation
IP_PUBLIC_SERVER=

# Cloudflare API Base URL (Usually don't change this)
# Format: https://api.cloudflare.com/client/v4
CLOUDFLARE_API_BASE_URL=https://api.cloudflare.com/client/v4

# Maximum number of retry attempts when API calls fail
# Recommended: 3-5 for production, 1-2 for testing
CLOUDFLARE_API_RETRY_ATTEMPTS=5

# Delay between retry attempts in milliseconds
# When a request fails, wait this long before trying again
# Use exponential backoff in code (1000, 2000, 4000, etc.)
CLOUDFLARE_RETRY_DELAY=1000

# Delay between consecutive API calls in milliseconds
# Prevents hitting Cloudflare's rate limits (1200 requests/5min free tier)
# Recommended: 1000ms (1 request/second) for safety
CLOUDFLARE_API_CALL_DELAY=1000

# Number of records per page for paginated API responses
# Cloudflare default: 20, max: 100 (some endpoints have different limits)
# Higher = fewer API calls for large datasets
CLOUDFLARE_PER_PAGE=50